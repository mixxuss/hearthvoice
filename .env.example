# Point these at your own servers. Every one must be on a network you control:
# the agent refuses to start otherwise.

SPARK_VIP=192.168.60.49

LLM_BASE_URL=http://192.168.60.49:19080/v1
LLM_MODEL=qwen3.5

NVIDIA_STT_SERVER=192.168.60.49:50051
NVIDIA_TTS_SERVER=192.168.60.49:50053
NVIDIA_TTS_VOICE=Magpie-Multilingual.EN-US.Brian

MQTT_HOST=127.0.0.1
MQTT_PORT=1883
HA_URL=http://127.0.0.1:8123

# Optional: a still image to use when no camera is available.
# VISION_IMAGE=eval/vision_fixture.jpg

# Optional: where libspeexdsp lives, if not in the usual places.
# SPEEX_LIB=/path/to/libspeexdsp.dylib
